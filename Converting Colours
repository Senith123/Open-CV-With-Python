import cv2
img1 = cv2.imread("Images\Videocollage2.jpg")
# # gray = cv2.cvtColor(img1, cv2.COLOR_BGR2GRAY)
# hsv = cv2.cvtColor(img1, cv2.COLOR_BGR2HSV)
# cv2.imshow("convert_image", hsv)
# cv2.waitKey(0)

# edges = cv2.Canny(img1, 300, 500)
# cv2.imshow("edges_image", edges)
# cv2.waitKey(0)
im2 = cv2.imread("Images\Videocollage3.jpg")
print(im2.shape)
r, c = im2.shape[0:2]
Matrix = cv2.getRotationMatrix2D((c/2 - 200, r/2 - 200), 90, 0.5)
rotate = cv2.warpAffine(im2, Matrix, (c, r))
cv2.imshow("rotate_image", rotate)
cv2.waitKey(0)
